/**
 * git version
 *
 * git init
 *
 * git config -l
 * git config --list
 *
 * git config --global user.name "Mohamed-Mayallo"
 * git config --global user.email "reckoning58@gmail.com"
 *
 * git remote add origin https://github.com/Mohamed-Mayallo/practical_git.git
 * git remote -v
 *
 * git status
 *
 * git add .
 * git add views/*
 *
 * git commit -m "First commit"
 * git commit -am "Commit and add"
 *
 * git push origin -u master
 *
 * git branch
 * git branch contacts
 * git checkout contacts
 * git checkout -b contacts
 * git branch -D contacts
 */
